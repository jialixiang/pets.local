<%= form_with(model: customer_preference, local: true) do |form| %>
  <% if customer_preference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_preference.errors.count, "error") %> prohibited this customer_preference from being saved:</h2>

      <ul>
      <% customer_preference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="field">
    <%= form.label :species %>
    <%= form.text_field :species %>
  </div>

  <div class="field">
    <%= form.label :breed %>
    <%= form.text_field :breed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
