<%= form_with(model: adoption, local: true) do |form| %>
  <% if adoption.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adoption.errors.count, "error") %> prohibited this adoption from being saved:</h2>

      <ul>
      <% adoption.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id %>
  </div>

  <div class="field">
    <%= form.label :pet_id %>
    <%= form.text_field :pet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
